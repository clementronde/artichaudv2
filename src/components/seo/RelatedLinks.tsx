'use client'

import type { RelatedLink } from './relatedLinksData'

type RelatedLinksProps = {
  title?: string
  eyebrow?: string
  links: RelatedLink[]
}

export default function RelatedLinks(_props: RelatedLinksProps) {
  void _props
  return null
}
